<div class="section no-pad-bot" id="index-banner">
  <div class="container">
    <h5 class="header center orange-text">Services</h5>
    <% services.each do |service| %>
        <h5><%= service.title %></h2>
        <p><%= service.description %></p>
    <% end %>
  </div>
</div>
